export default class GameScene extends Phaser.Scene {
  constructor() {
    super({ key: 'GameScene' });
  }

  create() {
    // Инициализация игрока, препятствий, бонусов и т.д.
  }

  update() {
    // Логика движения, столкновений и сбора бонусов
  }
}
